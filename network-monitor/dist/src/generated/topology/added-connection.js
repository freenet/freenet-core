"use strict";
// automatically generated by the FlatBuffers compiler, do not modify
var __createBinding =
  (this && this.__createBinding) ||
  (Object.create
    ? function (o, m, k, k2) {
        if (k2 === undefined) k2 = k;
        var desc = Object.getOwnPropertyDescriptor(m, k);
        if (
          !desc ||
          ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)
        ) {
          desc = {
            enumerable: true,
            get: function () {
              return m[k];
            },
          };
        }
        Object.defineProperty(o, k2, desc);
      }
    : function (o, m, k, k2) {
        if (k2 === undefined) k2 = k;
        o[k2] = m[k];
      });
var __setModuleDefault =
  (this && this.__setModuleDefault) ||
  (Object.create
    ? function (o, v) {
        Object.defineProperty(o, "default", { enumerable: true, value: v });
      }
    : function (o, v) {
        o["default"] = v;
      });
var __importStar =
  (this && this.__importStar) ||
  function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null)
      for (var k in mod)
        if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k))
          __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
  };
Object.defineProperty(exports, "__esModule", { value: true });
exports.AddedConnectionT = exports.AddedConnection = void 0;
const flatbuffers = __importStar(require("flatbuffers"));
class AddedConnection {
  constructor() {
    this.bb = null;
    this.bb_pos = 0;
  }
  __init(i, bb) {
    this.bb_pos = i;
    this.bb = bb;
    return this;
  }
  static getRootAsAddedConnection(bb, obj) {
    return (obj || new AddedConnection()).__init(
      bb.readInt32(bb.position()) + bb.position(),
      bb
    );
  }
  static getSizePrefixedRootAsAddedConnection(bb, obj) {
    bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
    return (obj || new AddedConnection()).__init(
      bb.readInt32(bb.position()) + bb.position(),
      bb
    );
  }
  from(optionalEncoding) {
    const offset = this.bb.__offset(this.bb_pos, 4);
    return offset
      ? this.bb.__string(this.bb_pos + offset, optionalEncoding)
      : null;
  }
  fromLocation() {
    const offset = this.bb.__offset(this.bb_pos, 6);
    return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
  }
  to(optionalEncoding) {
    const offset = this.bb.__offset(this.bb_pos, 8);
    return offset
      ? this.bb.__string(this.bb_pos + offset, optionalEncoding)
      : null;
  }
  toLocation() {
    const offset = this.bb.__offset(this.bb_pos, 10);
    return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
  }
  static startAddedConnection(builder) {
    builder.startObject(4);
  }
  static addFrom(builder, fromOffset) {
    builder.addFieldOffset(0, fromOffset, 0);
  }
  static addFromLocation(builder, fromLocation) {
    builder.addFieldFloat64(1, fromLocation, 0.0);
  }
  static addTo(builder, toOffset) {
    builder.addFieldOffset(2, toOffset, 0);
  }
  static addToLocation(builder, toLocation) {
    builder.addFieldFloat64(3, toLocation, 0.0);
  }
  static endAddedConnection(builder) {
    const offset = builder.endObject();
    builder.requiredField(offset, 4); // from
    builder.requiredField(offset, 8); // to
    return offset;
  }
  static createAddedConnection(
    builder,
    fromOffset,
    fromLocation,
    toOffset,
    toLocation
  ) {
    AddedConnection.startAddedConnection(builder);
    AddedConnection.addFrom(builder, fromOffset);
    AddedConnection.addFromLocation(builder, fromLocation);
    AddedConnection.addTo(builder, toOffset);
    AddedConnection.addToLocation(builder, toLocation);
    return AddedConnection.endAddedConnection(builder);
  }
  unpack() {
    return new AddedConnectionT(
      this.from(),
      this.fromLocation(),
      this.to(),
      this.toLocation()
    );
  }
  unpackTo(_o) {
    _o.from = this.from();
    _o.fromLocation = this.fromLocation();
    _o.to = this.to();
    _o.toLocation = this.toLocation();
  }
}
exports.AddedConnection = AddedConnection;
class AddedConnectionT {
  constructor(from = null, fromLocation = 0.0, to = null, toLocation = 0.0) {
    this.from = from;
    this.fromLocation = fromLocation;
    this.to = to;
    this.toLocation = toLocation;
  }
  pack(builder) {
    const from = this.from !== null ? builder.createString(this.from) : 0;
    const to = this.to !== null ? builder.createString(this.to) : 0;
    return AddedConnection.createAddedConnection(
      builder,
      from,
      this.fromLocation,
      to,
      this.toLocation
    );
  }
}
exports.AddedConnectionT = AddedConnectionT;
//# sourceMappingURL=added-connection.js.map
