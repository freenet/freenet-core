// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



export class Authenticate implements flatbuffers.IUnpackableObject<AuthenticateT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):Authenticate {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsAuthenticate(bb:flatbuffers.ByteBuffer, obj?:Authenticate):Authenticate {
  return (obj || new Authenticate()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsAuthenticate(bb:flatbuffers.ByteBuffer, obj?:Authenticate):Authenticate {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new Authenticate()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

token():string|null
token(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
token(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startAuthenticate(builder:flatbuffers.Builder) {
  builder.startObject(1);
}

static addToken(builder:flatbuffers.Builder, tokenOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, tokenOffset, 0);
}

static endAuthenticate(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // token
  return offset;
}

static createAuthenticate(builder:flatbuffers.Builder, tokenOffset:flatbuffers.Offset):flatbuffers.Offset {
  Authenticate.startAuthenticate(builder);
  Authenticate.addToken(builder, tokenOffset);
  return Authenticate.endAuthenticate(builder);
}

unpack(): AuthenticateT {
  return new AuthenticateT(
    this.token()
  );
}


unpackTo(_o: AuthenticateT): void {
  _o.token = this.token();
}
}

export class AuthenticateT implements flatbuffers.IGeneratedObject {
constructor(
  public token: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const token = (this.token !== null ? builder.createString(this.token!) : 0);

  return Authenticate.createAuthenticate(builder,
    token
  );
}
}
