[package]
name = "locutus-stdlib"
version = "0.0.0"
edition = "2021"
rust-version = "1.58.0"

[dependencies]
arbitrary = { version = "1", optional = true }
arrayvec = { version = "0.7", features = ["serde"] }
blake2 = { version =  "0.10", features = [ "std" ] }
hex = "0.4"
once_cell = "1"
serde = "1"
thiserror = "1"

# internal
locutus-macros = { path = "../locutus-macros" }

[dev-dependencies]
arbitrary = "1"
bincode = "1"
wasmer = { version = "^2.0", features = [ "sys-default", "wat"] }
wasmer-wasi = "*"
rand = "0.8"

[features]
testing = ["arbitrary"]
